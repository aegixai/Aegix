{
  "name": "aegix-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npm install --legacy-peer-deps && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
